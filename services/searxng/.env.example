# SearXNG'in kendi sırrı. Kök `.env` uygulamanın yapılandırması; bu ona ait değil.
# `.env` gitignore'da (kök `.gitignore`'daki `.env` deseni her dizinde geçerli).
#
#   cp .env.example .env && sed -i "s/DEGISTIR/$(openssl rand -hex 32)/" .env
SEARXNG_SECRET=DEGISTIR
